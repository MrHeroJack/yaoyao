# 测试报告

## 覆盖范围
- 单元/组件：上传校验、进度显示、事件图片插入
- 集成：签名接口模拟与上传流程打通
- 端到端：管理员登录、添加事件、上传与预览、删除

## 结果摘要
- 单元测试：通过（本地运行 `npm run test`）
- 集成测试：通过（MSW 拦截签名接口）
- E2E：通过（`npm run e2e`，需先 `npm run dev`）

## 性能
- 并发 3 上传下 UI 保持流畅，进度更新每 100ms
- 移动端（慢 4G）下平均上传时间与失败重试策略可在 `src/admin/hooks/useUpload.ts` 配置

